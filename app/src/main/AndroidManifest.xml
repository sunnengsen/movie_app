<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/panda"
        android:label="MovieApp"
        android:roundIcon="@drawable/panda"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:targetApi="31" >

        <activity
            android:name=".ui.element.activity.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Material3.Dark.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.element.activity.LoginActivity"
            android:exported="true" />

        <activity
            android:name=".ui.element.activity.PlayMovieActivity"
            android:exported="true" />

    </application>

</manifest>